#include "sorter.h"
#pragma once

/// Class, for sorting array with bubble sort
class BSorter : public Sorter
{
    public:
    /// Procedure, for sorting array with bubble sort
        void sort(int * inputArray, int size);
};
